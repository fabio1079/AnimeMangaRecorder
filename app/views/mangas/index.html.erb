<h1>Listing mangas</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Volume</th>
      <th>Chapter</th>
      <th>Chapter title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mangas.each do |manga| %>
      <tr>
        <td><%= manga.title %></td>
        <td><%= manga.volume %></td>
        <td><%= manga.chapter %></td>
        <td><%= manga.chapter_title %></td>
        <td><%= link_to 'Show', manga %></td>
        <td><%= link_to 'Edit', edit_manga_path(manga) %></td>
        <td><%= link_to 'Destroy', manga, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Manga', new_manga_path %>
